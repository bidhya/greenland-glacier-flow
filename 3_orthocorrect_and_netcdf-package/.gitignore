# Project-specific: Old/archived code
*_debug.py

# Active config (user-specific, not tracked - use config_template.py)
# lib/config.py

# Docs/agent guide (ignore local edits; note: already tracked unless removed from index)
Step3_Agents.md  

# Documentation folder (intended for local drafts and preview) - can be ignored during incremental commits
docs/

# Local HPC sync script (experimental, keep untracked until finalized)
sync_to_hpc.sh
processing_chain/4c_netcdf_stack_landsat_sentinel_combined.bak.py
bak.orthocorrect_netcdf-package.py
